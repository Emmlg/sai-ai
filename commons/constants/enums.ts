export enum STATUS {
    ACTIVO = "activo",
    INACTIVO = "inactivo",
    SUSPENDIDO = "suspendido",
}

export enum EXPERIENCE_LEVEL {
    NOVATO = "novato",
    INTERMEDIO = "intermedio",
    EXPERTO = "experto",
}

export enum ATTENTION_LEVEL {
    OPTIMO = "optimo",
    MODERADO = "moderado",
    CRITICO = "critico",
}

export enum RISK_LEVEL {
    BAJO = "bajo",
    MEDIO = "medio",
    ALTO = "alto",
}

export enum CURRENT_VIEW {
    MONITOR = "monitor",
    DRIVERDETAIL = "driver-detail",
    HISTORY = "history",
    SUGGESTIONS = "suggestions",
}

export enum vehicleType {
    CAMION_CARGA = "camión de carga"
}